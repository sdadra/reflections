git remote
git remote add origin https://github.com/sdadra/reflections.git
git remote -v
	
git config --global credential.helper wincred

git pull origion master

Why might you want to always pull changes manually rather than having Git automatically stay up-to-date with your remote repository?
-may not give the working code working code
-merge issues

git clone https://github.com/sdadra/recipes.git

Describe the differences between forks, clones, and branches. When would you use one instead of another?
-Forks: 
-Clone: copy repository
-Branches: local changes

bash sarah_changes.sh https://github.com/sdadra/reflections.git

git fetch [get remote changes on the local machine for diff and all]

What is the benefit of having a copy of the last known state of the remote stored locally?

git branch different-oil

How would you collaborate without using Git or GitHub? What would be easier, and what would be harder?
-dropbox , gdrive
-issues: no control over commits- so WIP copies also get shared